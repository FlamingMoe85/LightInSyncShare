#include "PositionManipulator.h"

PositionManipulator::PositionManipulator()
{

}

void PositionManipulator::Calculate(rangePosition_t &_pos)
{
    /*
    for(Function_Base* f : myFunctions)
    {
        if(_pos >= f->GetBottomX())
        {
            _pos = f->AlterPosition(_pos);
            break;
        }
    }
    */
}
